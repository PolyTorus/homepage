pre-commit:
  piped: true
  commands:
    prettier:
      priority: 1
      run: |
        bun run format
      stage_fixed: true

    eslint:
      priority: 2
      run: |
        bun run lint:fix

post-merge:
  commands:
    install-deps:
      run: |
        bun install
